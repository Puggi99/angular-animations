import { FatButtonDirective } from "./fat-button.directive";


describe('FatButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new FatButtonDirective();
    expect(directive).toBeTruthy();
  });
});
